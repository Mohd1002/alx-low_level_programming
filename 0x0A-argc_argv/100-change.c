#include <stdio.h>
/**
 */
int main(int argc, char *argv[])
{
	int c = 25, ch = 10, cha = 5 , chan = 2 , chang = 1;
	if (argc != 1)
	{
		printf("Error\n")
		return (1);
	}
	if (argv[1] < 0)
	{
		printf("0\n"
		return (0);
	}
	
