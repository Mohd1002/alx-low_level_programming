#include <stdio.h>
#include <stdlib.h>
/**
 * main - entry point
 * Return: returns 0 in success
 * @argc: the number of arguments entered on the commandline
 * @argv: the array of arg on the commandline
 */
int main(int argc, char *argv[])
{
	puts(argv[argc - argc]);
	return (0);
}
