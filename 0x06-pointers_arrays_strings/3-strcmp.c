#include <stdio.h>
#include <string.h>
/**
 * _strcmp - compares two strings
 * @s1: string one
 * @s2: string two
 * Return: returns the value of _strcmp
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
