#include <stdio.h>
#include <string.h>
/**
 * _strcat - appends src to the end of dest
 * @dest: string one
 * @src: string two
 * Return: the value fo _strcat
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
