#include <stdio.h>
#include <ctype.h>
#include "main.h"
#include <stdlib.h>
/**
 * _abs - computes the absolute value of an integer
 * Return: returns the answer of _abs
 * @n: the integer being valued
 */
int _abs(int n)
{
	return (abs(n));
}
