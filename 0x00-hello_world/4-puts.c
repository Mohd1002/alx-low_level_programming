#include<stdio.h>
/**
 * main - Entry point it prints a text
 * Return: Always 0 (Success)
 */
int main(void)

{
	char answer[100] = "\"Programming is like building a multilingual puzzle";

	puts(answer);
	return (0);
}
